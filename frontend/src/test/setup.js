// Setup global de Vitest para tests de componentes React.
// Registra los matchers extendidos de @testing-library/jest-dom
// (toBeInTheDocument, toHaveAttribute, etc.) en cada archivo de test.
import '@testing-library/jest-dom';
